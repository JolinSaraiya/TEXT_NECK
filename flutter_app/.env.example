# Environment Variables Template for Teammates
# Rename this file to .env and fill in your values!

# Example placeholder if we need a custom backend in the future
API_URL=https://your-custom-backend.com/api

# Feature flag for testing
DEBUG_MODE=true

# (Note: Firebase handles its own keys automatically via the firebase_options.dart file)
